# Later Text2Voice — Python dependencies
#
# Install for local testing:
#   pip install -r requirements.txt
#
# For Lambda deployment, install into a local folder first (deploy.sh does this):
#   pip install -r requirements.txt -t package/
#
# boto3 is pre-installed in the Lambda Python runtime, but listed here so
# local testing works without any extra steps.

requests>=2.31.0
pdfminer.six>=20221105
beautifulsoup4>=4.12.3
boto3>=1.34.0
